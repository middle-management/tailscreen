# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/

# Built C library in upstream (created by make c-archive)
upstream/libtailscale/libtailscale.a
upstream/libtailscale/libtailscale.h

# Patch marker file (historical) and the per-submodule-commit build stamp
.patches-applied
.lib-built-*

# Build outputs
*.o
*.dylib
*.so

# macOS
.DS_Store

# Temporary files
*.swp
*.swo
*~

# VS Code
.vscode/

# Note: Sources/, lib/, and include/ are symlinks - they are tracked!

# Per-submodule-commit build stamp (see the Makefile fix on the phase-0 PR)
.lib-built-*
