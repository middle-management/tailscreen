#ifndef TS_CDBUSSYS_SHIM_H
#define TS_CDBUSSYS_SHIM_H

// libdbus, for the portal handshake. Only the C targets include this — the
// Swift wrapper never sees a DBusMessage.
#include <dbus/dbus.h>

#endif
